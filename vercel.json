{
    "version": 2,
    "functions": {
        "server.js": {
            "runtime": "nodejs20.x",
            "includeFiles": "routes/**|models/**|config/**|middleware/**|*.js"
        }
    },
    "routes": [
        {
            "src": "/(.*)",
            "dest": "server.js"
        }
    ]
}