{
    "version": 2,
    "functions": {
        "server.js": {
            "includeFiles": "routes/**|models/**|config/**|middleware/**|*.js"
        }
    },
    "routes": [
        {
            "src": "/(.*)",
            "dest": "server.js"
        }
    ]
}